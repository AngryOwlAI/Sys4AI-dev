# Generic /init

`/init` is the portable Sys4AI front door template for target-system definition and adoption.

It classifies greenfield and brownfield situations, inspects evidence before asking questions, and asks for approval before creating discovery records, Product Requirements Documents, system requirements, implementation plans, scaffolds.

The brownfield first pass is read-only and should produce an evidence summary before any file is written.
